name: 4-EventTriggers
on:
  schedule:
    - cron: '*/1 0 * * *' #run evey 1 minutes 
  #push:
  #  branches:
  #    - devbranch
jobs:
  my-first-job:
    name: my first job
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world" 